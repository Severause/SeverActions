customCategory: arrest
name: RejectEscortPlea
description: "The escorting guard hears the prisoner's plea out and isn't having any of it — the story's too thin, the bribe too cheap, or they've heard this one too many times before. They jerk the prisoner forward and the march to the cells resumes. Only use when you are the escorting guard and a prisoner has just appealed to you mid-escort."
questEditorId: SeverActions
scriptName: SeverActions_Arrest
executionFunctionName: RejectEscortPlea_Internal
parameterMapping:
  - type: speaker
    name: akGuard
    description: the escorting guard refusing the plea
enabled: true
defaultPriority: 9
eligibilityRules:
  - conditions:
      - decoratorName: "is_in_combat"
        arguments: ["currentActor"]
        comparisonOperator: "=="
        expectedValue: false
    logicalOperator: "AND"
    required: true
  - conditions:
      - decoratorName: "is_busy"
        arguments: ["currentActor"]
        comparisonOperator: "=="
        expectedValue: true
    logicalOperator: "AND"
    required: true
  - conditions:
      - decoratorName: "busy_reason"
        arguments: ["currentActor"]
        comparisonOperator: "=="
        expectedValue: "arrest"
    logicalOperator: "AND"
    required: true
  # Authority gate — aligns with the rest of the arrest action set
  # (arrestnpc, acceptpersuasion, etc.). The previous 9-faction OR ladder
  # silently dropped Jarl/Housecarl coverage, so an arrest performed by a
  # Jarl/Housecarl couldn't enter the escort-plea side state. is_guard_or_authority
  # collapses that ladder + adds the missing two factions in one decorator.
  - conditions:
      - decoratorName: "is_guard_or_authority"
        arguments: ["currentActor"]
        comparisonOperator: "!="
        expectedValue: ""
    logicalOperator: "AND"
    required: true
  - conditions:
      - decoratorName: "get_faction_rank"
        arguments: ["currentActor", "SexLabAnimatingFaction"]
        comparisonOperator: "<"
        expectedValue: 0
    logicalOperator: "AND"
    required: true
  - conditions:
      - decoratorName: "get_faction_rank"
        arguments: ["currentActor", "OStimExcitementFaction"]
        comparisonOperator: "<"
        expectedValue: 0
    logicalOperator: "AND"
    required: true
  # Mirror the acceptpersuasion / rejectpersuasion guard: a speaker on a
  # cross-cell dispatch errand should never be eligible for escort-plea
  # actions (which belong to the same-cell escort flow). Redundant with
  # busy_reason == "arrest" in practice but matches the rest of the
  # arrest YAML set so eligibility stays consistent.
  - conditions:
      - decoratorName: "is_in_faction"
        arguments: ["currentActor", "SeverActions_DispatchFaction"]
        comparisonOperator: "=="
        expectedValue: false
    logicalOperator: "AND"
    required: true
