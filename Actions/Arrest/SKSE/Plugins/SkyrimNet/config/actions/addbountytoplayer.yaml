customCategory: arrest
name: AddBountyToPlayer
description: "Use this first when the speaker witnessed {{ player.name }} commit a crime — ArrestPlayer only works once a bounty exists on the books. The guard logs the crime against {{ player.name }}'s name in this hold. Guards may also stack bounty at their own discretion for backtalk, threats, or suspicious behavior."
questEditorId: SeverActions
scriptName: SeverActions_ArrestBounty
executionFunctionName: AddBountyToPlayer_Internal
parameterMapping:
  - type: speaker
    name: akGuard
    description: the guard writing the charge into the books
  - type: dynamic
    name: bountyAmount
    description: "how much gold the offense is worth on the books — assault 40-200, theft 75-300, murder 1000-3000, trespass 75-150, pickpocket 25-175, contempt 25-100, abuse_of_power 50-250"
  - type: dynamic
    name: crimeType
    description: "what they're being charged with: assault, theft, murder, trespass, pickpocket, contempt, or abuse_of_power"
enabled: true
defaultPriority: 7
eligibilityRules:
  - conditions:
      - decoratorName: "is_in_combat"
        arguments: ["currentActor"]
        comparisonOperator: "=="
        expectedValue: false
    logicalOperator: "AND"
    required: true
  - conditions:
      - decoratorName: "is_busy"
        arguments: ["currentActor"]
        comparisonOperator: "=="
        expectedValue: false
    logicalOperator: "AND"
    required: true
  # Replaced an 11-condition OR ladder (9 GuardFactionX + JobJarlFaction
  # + JobHousecarlFaction) with the single native is_guard_or_authority
  # decorator. Same semantics — see SkyrimNetBridge.h for the cached
  # faction-pointer set, plus mod-added holds via HoldResolver.
  - conditions:
      - decoratorName: "is_guard_or_authority"
        arguments: ["currentActor"]
        comparisonOperator: "!="
        expectedValue: ""
    logicalOperator: "AND"
    required: true
  - conditions:
      - decoratorName: "get_faction_rank"
        arguments: ["currentActor", "SexLabAnimatingFaction"]
        comparisonOperator: "<"
        expectedValue: 0
    logicalOperator: "AND"
    required: true
  - conditions:
      - decoratorName: "get_faction_rank"
        arguments: ["currentActor", "OStimExcitementFaction"]
        comparisonOperator: "<"
        expectedValue: 0
    logicalOperator: "AND"
    required: true
  - conditions:
      - decoratorName: "is_in_faction"
        arguments: ["currentActor", "SeverActions_DispatchFaction"]
        comparisonOperator: "=="
        expectedValue: false
    logicalOperator: "AND"
    required: true
